import 'package:flutter/material.dart';

class WorkoutPlans extends StatelessWidget {
  const WorkoutPlans({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       
      body: const Center(child: Text('egmner')),
    );
  }
}